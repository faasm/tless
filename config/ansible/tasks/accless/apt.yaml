- name: "Install APT depdencencies"
  become: yes
  apt:
    name:
      - build-essential
      - libfontconfig1-dev
      - libjsoncpp-dev
      - libssl-dev
      - pkg-config
      - python3-venv
    update_cache: yes
