- name: "Install APT depdencencies"
  become: yes
  apt:
    name:
      - build-essential
      - cmake
      - libfontconfig1-dev
      - libssl-dev
      - ninja-build
      - pkg-config
      - python3-venv
    update_cache: yes
